import React from 'react'
import { Card } from 'primereact/card';

export const CarritoLeno = () => {
    return (
        <>
            {/* <Card footer = {footerTemplate}>
                <div className='grid'>
                    <div class="col-10">
                        <div class='col-fixed flex' style={{width: "30%"}}>
                            {book.imagenProd ? (<img  className="w-full" src={require('../../images/' + book.imagenProd + '.png')} style={{display: "flex", objectFit:"contain"}}/>) : null}
                        </div>
                        <div class="col-12">
                            <p  style={{fontWeight: 'bold' }}>{book.titleProd ? book.titleProd : "Carrito vacío"}</p>
                        </div>
                    </div>   
                    <div class="col-2">
                        <p  style={{ color: 'red', fontWeight: 'bold' }}>{book.priceProd ? "$" + book.priceProd : ""}</p>
                    </div>  
                </div>
            </Card> */}
        </>
    )
}