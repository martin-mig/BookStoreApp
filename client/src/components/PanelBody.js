import React from 'react';
import { CheckboxInput } from './CheckboxInput';
import { FileTable } from './FileTable';


export const PanelBody = (selectedItem ) => {
console.log("Este es el selectedItem " + JSON.stringify(selectedItem) )
    return (
        <>
        
           
        </>
    )

}